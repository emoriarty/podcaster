const initialState = {
  country: 'es',
  language: 'es_ES'
}

const settings = (state = initialState, action) => {
  switch (action.type) {
  default:
    return state
  }
}

export default settings
